# Arithmetic example
#
# Implements simple arithmetic operation

contact arithmetic:
    let sum: num = 0

    def addition(x: num, y: num, z: num) -> num:
        sum =  x + y + y
        return sum


let var = addition()
print(var.addition(1, 2, 3))
