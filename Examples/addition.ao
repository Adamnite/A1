# Arithmetic example
#
# Implements simple arithmetic operation

contract Addition:
    let sum: num = 0

    def add(x: num, y: num) -> num:
        sum =  x + y
        return sum


let var = Addition()
print(var.add(1, 2))
