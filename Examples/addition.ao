# Arithmetic example
#
# Implements simple arithmetic operation

contract Addition:
    let sum: num = 0

    def add(self, x: num, y: num) -> num:
        self.sum =  x + y
        return self.sum


let var = Addition()
print(var.add(1, 2))
